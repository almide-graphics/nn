---
title: "自作言語でLLM推論エンジンを書いて、llama.cppと同じ土俵で殴り合うまで"
emoji: "⚔️"
type: "tech"
topics: ["llm", "rust", "compiler", "simd", "自作言語"]
published: false
---

<!-- DRAFT v0.1 — 決着ベンチの数字（§6）はプレースホルダ。確定後に差し替え。 -->

## TL;DR

- 自作言語 **Almide**（pure-Rustコンパイラ、Rust/WASMマルチターゲット）で
  Qwen3-0.6B の推論エンジンをフルスクラッチした
- **速さの前に正しさのゲート**を置いた: HuggingFace fp32 を審判に、
  logits 相対誤差 3.5e-06 / greedy 一致を 20 プロンプトで通してから最適化開始
- 0.025 tok/s → 25+ tok/s（**1000倍**）。最後は llama.cpp と同一マシン・
  同一モデル・同一量子化（Q8_0）で交互ベンチ
- 途中で分かったこと: **llama.cpp 自身も HF fp32 と greedy がズレる**。
  「完全一致」は王者すら満たさない基準だった
- llama.cpp の AVX2 カーネルを読んだら**内積は同一レシピ**だった。
  差は内積ではなく「グルー」— トークンあたり 14.5 万回のスカラー libm 呼び出し
- tokenizer も純 Almide で byte-level BPE を実装、HF のトークン化と 20/20 一致。
  テキストin→ストリーミングout のチャットが動く

## 0. なぜ自作言語で LLM エンジンか

Almide は「LLM が最も正確に書ける言語」を目標に設計している
（設計指標は modification survival rate — LLM がコードを修正したとき
壊さず生き残る率）。その実証として、世で最も性能要求が苛烈な部類の
システムソフトウェア — LLM 推論エンジン — を選んだ。
言語が本物なら、これが書けるはずだ。逆に書けないなら、その過程で
言語の穴が全部露出する。実際、露出した（§7）。

## 1. L1: 正しさのゲートを最初に置く

最適化の前に、間違ったものを速くしても意味がない。そこで最初の
マイルストーンを「速さ」ではなく **logits parity** にした。

仕組み:

1. **GGUF を自前でエクスポート**する（HF の重みから f32 で）。
   コミュニティの量子化済み GGUF を使うと、誤差が「自分のバグ」なのか
   「他人の量子化」なのか切り分けられなくなる。f32 で出せばノイズはゼロ。
2. 固定 20 プロンプト（英日コード混在、改行入り）を HF (torch fp32) に
   通して、トークン ID と最終 logits をダンプ
3. 同じトークン ID を Almide エンジンに食わせ、logits をテキストで突き合わせ
4. 合格基準: argmax 一致 + 相対誤差 < 1e-3（実測は 3.5e-06）+
   greedy 16 トークン一致

このゲートのおかげで、以降の最適化（量子化、SIMD、並列化、GPU化）の
すべてのステップで「壊れたら即分かる」状態を維持できた。
回帰検知はカナリアプロンプト1本（"The capital of France is" → 既知の
40トークン列）で数秒。

## 2. 番狂わせ: 王者も HF と一致しない

Q8_0 化した後、greedy の出力が HF fp32 と 1 トークンだけズレる現象を
追っていて、衝撃の事実に当たった。**llama.cpp 自身も同条件でズレる**。

Qwen3-0.6B に首都を列挙させると、HF fp32 は 4 番目に China/Beijing を
出すが、llama.cpp の Q8_0 greedy は Germany/Berlin を出す。原因は
量子化誤差による logits のニアタイ（ほぼ同点の 2 候補）で、これは
Q8_0 を選んだ時点で避けられない。

つまり「HF と完全一致」は、業界標準の llama.cpp すら満たさない基準
だった。ここで品質バーを再定義した:

> **parity band + coherence**: ニアタイ位置以外は一致、かつ出力が
> 首尾一貫していること。タイの分岐位置はカナリアで把握しておく。

なお後日談として、チャットテンプレート込みの greedy では、うちの
エンジンは HF fp32 と**間違いまで含めて一字一句一致**する
（0.6B は「日本の首都は大阪市です」と答える。HF も同じく大阪と答える。
エンジンの正しさとモデルの賢さは別の話だ）。

## 3. 速度の旅: 0.025 → 25+ tok/s

L1 通過時点の素朴な実装は 0.025 tok/s だった。1000 倍の旅程の主な駅:

| 改善 | tok/s | 学び |
|---|---|---|
| 素朴な実装（f64 Matrix、毎回デコード） | 0.025 | まず正しく |
| 重みを bytes 常駐に（GGUF をメモリに置いたまま読む） | ~0.4 | コピーが支配的 |
| 言語側: RcCow 導入（clone = Rc バンプ） | ~5 | **13GB/token** が消えた |
| AVX2 整数内積（Q8×Q8: sign+maddubs+madd） | ~17 | llama.cpp と同一レシピ |
| lockstep トークン実行器（1 token = 1 rayon scope、スピンバリア） | ~25 | スレッド起床コストの除去 |
| 超越関数の SIMD 化（exp 多項式、RoPE テーブル共有） | 25+ | 真の敵はグルーだった |

最大の発見は最後の行。llama.cpp の `ggml-cpu/arch/x86/quants.c` を
読んで内積カーネルを突き合わせたら、**完全に同じレシピ**だった
（符号トリック + `maddubs` + `madd`、アンロールなし、プリフェッチなし）。
ならなぜ向こうが速い？ プロファイルを取り直すと、差は内積の外側にあった:

- silu の `expf` ×86k 回/token
- RoPE の `sincos` ×43k 回/token
- softmax、量子化、residual 加算のスカラーループ

合計 **約 14.5 万回/token のスカラー libm 呼び出し**。これを Cephes 系
多項式の AVX2 実装とトークン内テーブル共有で潰したら並んだ。
教訓: **カーネルを磨く前にグルーを数えろ**。

## 4. ベンチ方法論: サーマルに殺されない

同一マシン（i7-7700, AVX2+FMA, VNNI なし）での比較で、最初に得た
llama.cpp の数字は 28.2 tok/s だった。これは**間違い**で、冷えた状態では
39.4 出る。このマシンは熱で約 40% 落ちる。

以降のルール:

1. **25 分アイドル後にのみ計測開始**（コンパイルも禁止）
2. **交互に実行**（A→B→A→B→A→B）。両者が同じ熱履歴を踏む
3. **絶対値でなく比率**を見る。スロットリングは両者に等しくかかる
4. `ps aux` で他プロセスの混入を確認してから回す

これをやらないと ±50% 平気でブレる。シングルランの絶対値を持ち出す
ベンチ記事は（うちも含め）疑ってかかるべき。

## 5. tokenizer も純 Almide で

エンジンがトークン ID の入出力だけでは「ベンチ」であって「製品」では
ない。Qwen3 の byte-level BPE（GPT-2 系）を純 Almide で実装した:

- GGUF の `tokenizer.ggml.tokens/merges`（15.2 万語 + 15.1 万マージ）を
  ソート済み配列 + 二分探索で保持
- pretokenizer は Qwen2/3 の正規表現を選択肢順込みで手書きステートマシン化
  （`\s+(?!\S)` のバックトラックまで再現）
- 受け入れ基準は例によって HF: **20/20 プロンプトでトークン化完全一致** +
  decode 往復一致

これでチャットテンプレート（`<|im_start|>`、空 `<think>` ブロック）を
組んで、テキスト入力 → UTF-8 安全なストリーミング出力が動く。
モデルロード込み 3 秒で起動する。

## 6. 決着ベンチ

計測条件: Qwen3-0.6B Q8_0、tg64（64トークン生成）、数時間アイドルの
冷機から開始、i7-7700（物理4コア、両者4スレッド）、他プロセスなし。

**ラウンド1: ABABAB（毎ペア llama.cpp 先行）**

| pair | llama.cpp | almide | 比 |
|---|---|---|---|
| 1 | 37.33 ±0.31 | 35.37 | 94.7% |
| 2 | 31.74 ±0.58 | **32.30** | **101.8%** |
| 3 | 36.04 ±0.26 | 31.59 | 87.6% |

ただしこの形式は**毎回 llama.cpp が先（=より冷えた状態）で走る**順序
バイアスがある。そこで——

**ラウンド2: ABBA（順序を反転して相殺）**

| 順 | エンジン | tok/s |
|---|---|---|
| 1（最冷） | almide | 35.99 |
| 2 | llama.cpp | 38.06 ±0.03 |
| 3 | llama.cpp | 32.55 ±1.56 |
| 4（最熱） | almide | 35.18 |

位置を対にして平均すると **almide 35.59 vs llama.cpp 35.31** — 統計的互角。
全10本の通算平均は llama.cpp 35.1 vs almide 34.1（97%）。

注目はラウンド2の3〜4本目: llama.cpp は熱で 38.1→32.6（-14%）まで
落ちたのに対し、almide は最も熱い4番手でも 35.2 を維持した。lockstep
実行器を**物理コアのみ**で回す設計（HT siblings はスピンで互いを飢え
させるので使わない）が、熱制約下のクロック低下に対して強く出ている。

結論: **フルスクラッチ自作言語エンジンが、llama.cpp と同条件で統計誤差内の互角**。
ニアタイの greedy 分岐（§2）も含め、もはや両者を分ける要素は量子化タイと
熱履歴だけになった。

## 7. 副産物: 言語が鍛えられた

このプロジェクトで Almide コンパイラに見つけて報告/修正した問題は
2 日間で 10 件超。代表例:

- ユーザー関数境界とクロージャ捕獲の**値渡し深 clone**
  （辞書 60 万文字列を lookup 毎に複製 → 71s。レコードパラメータの
  参照借用がコンパイラに入って 2.5s に）
- クロージャが捕獲した `Bytes` を**生成時に丸ごと clone**
  （2.4GB バッファで顕在化、ロード時間の 84%）
- WASM ターゲットの RC 管理バグ（`?? ""` フォールバック + ループで
  ヒープ破壊）— 15 行まで最小化して報告
- リンクされたモジュールのレコード型が fold ラムダを通ると
  名前解決が壊れる — 16 行で最小化して報告

「LLM が書く言語」の開発手法として、**極端なドッグフーディング**は
ベンチマークスイートより速くバグを露出させる。性能要求が苛烈な
ワークロードは、言語のあらゆる怠慢を罰してくれる。

## 8. 次にやること

- **ブラウザ移植**: エンジンの WGSL カーネルは wgpu/WebGPU 共用設計に
  してある。WASM + WebGPU で「URL を開くとローカル LLM チャット」へ
- その先に、音声（Whisper も nn に実装済み）+ VRM アバター + UI を
  全部 Almide スタックで積んだ**完全ローカル AI キャラクター**

---

*リポジトリ: github.com/almide-graphics/nn（エンジン）、
github.com/almide/almide（言語）。再現手順は repo の
tools/ と examples/ に全部入っている。*
